package com.example.mobile_app_project.data.remote

/**
 * Skeleton for remote API definitions (Open-Meteo).
 * Define endpoints and DTOs later.
 */
interface WeatherApi {
    // TODO: add endpoints e.g., forecast and geocoding
}
